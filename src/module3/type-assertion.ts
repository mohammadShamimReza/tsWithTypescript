// let emni: any;

// emni = 'next lavel web developement';

// (emni as string).length;

// function kgToGram(param: string | number): string | number | undefined { 
//     if (typeof param === 'string') { 
//         const g = parseFloat(param) * 1000;
//         return ` the converted result is ${g}`;
//     }
//     if (typeof param === 'number') { 
//         return param * 1000;
//     }
// }

// const resultToBeNumber = <number>kgToGram(1000);

// const resultToBeString = <string>kgToGram('2000');



// type customErrorType = {
//     message: string;
// }

// try {
    
// } catch (error) {
//     console.log((error as customErrorType).message);
// }